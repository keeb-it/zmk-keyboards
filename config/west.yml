manifest:
  remotes:
    - name: upstream
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: upstream
      revision: main
      import: app/west.yml
  self:
    path: config
